import { UnderConstruction } from '@/components/under-construction';

export default function SettingsPage() {
  return (
    <UnderConstruction
      title="Configuración"
      description="Administra la configuración general del panel de administración."
    />
  );
}
